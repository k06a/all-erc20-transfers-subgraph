{
  "name": "erc20-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ k06a/all-erc20-transfers",
    "create-local": "graph create --node http://localhost:8020/ k06a/all-erc20-transfers",
    "remove-local": "graph remove --node http://localhost:8020/ k06a/all-erc20-transfers",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 k06a/all-erc20-transfers"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
